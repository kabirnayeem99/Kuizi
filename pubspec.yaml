name: flutter_quiz_app
description: a cross platform quiz application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  awesome_dialog: ^1.2.1
  cupertino_icons: ^1.0.0
  dartz: ^0.9.2
  data_connection_checker: ^0.3.4
  equatable: ^1.2.5
  flutter_bloc: ^6.0.6
  get_it: ^5.0.1
  html_unescape: any
  http: ^0.12.2
  random_color: ^1.0.5
  shared_preferences: ^0.5.12+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/
